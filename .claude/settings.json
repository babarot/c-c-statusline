{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$(jq -r '.tool_input.command')\" | grep -qE '^git push'; then deno test --allow-read --allow-env --allow-run --allow-write src/; fi"
          }
        ]
      }
    ]
  }
}
