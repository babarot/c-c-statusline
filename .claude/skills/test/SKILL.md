---
description: Run all tests
allowed-tools: Bash(deno test:*)
---

# Test

Run all tests:

```
deno test --allow-read --allow-env --allow-run --allow-write src/
```

Report the results to the user.
